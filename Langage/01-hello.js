/**
 *
 * Additionne 2 params
 * @param {number} a Le premier nb
 * @param {number} b Le 2e nb
 * @returns {number} La somme
 */
const sum = (a, b) => a + b;

for (let i = 0; i < 3; i++) {
  console.log(sum('1', '2'));
}
